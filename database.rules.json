{
  "rules": {
    ".read": false,
    ".write": false,

    "events": {
      "$eventId": {
        ".read": true,
        ".write": "(!data.exists()) || (auth != null && (data.child('adminUid').val() === auth.uid || ((!data.hasChild('adminUid') || data.child('adminUid').val() === null) && newData.child('adminUid').val() === auth.uid)))"
      }
    },
    "phones": {
      "$phone": {
        ".read": "auth != null",
        "$eventCode": {
          ".write": "auth != null && (!data.exists() || data.child('uid').val() === auth.uid) && (!newData.exists() || newData.child('uid').val() === auth.uid)"
        }
      }
    }
  }
}