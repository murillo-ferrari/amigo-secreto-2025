{
  "rules": {
    ".read": false,
    ".write": false,

    "events": {
      "$eventId": {
        ".read": true,
        ".write": "auth != null && ((!data.exists() && newData.child('adminUid').val() === auth.uid) || (data.exists() && data.child('adminUid').val() === auth.uid))"
      }
    },
    "phones": {
      "$phone": {
        ".read": "auth != null",
        "$eventCode": {
          ".write": "auth != null && (!data.exists() || data.child('uid').val() === auth.uid) && (!newData.exists() || newData.child('uid').val() === auth.uid)"
        }
      }
    }
  }
}